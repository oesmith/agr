# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

twitter_one:
  user: one
  remote_id: t1
  type: TwitterStream
  config: <%= TwitterConfig.new(oauth_token: "tok", oauth_secret: "sec").to_yaml.inspect %>
  profile: <%= TwitterProfile.new(screen_name: "one", real_name: "User One", profile_image: "http://example.com/twitter/one.png").to_yaml.inspect %>

twitter_two:
  user: two
  remote_id: t2
  type: TwitterStream
  config: <%= TwitterConfig.new(oauth_token: "tok", oauth_secret: "sec").to_yaml.inspect %>
  profile: <%= TwitterProfile.new(screen_name: "two", real_name: "User Two", profile_image: "http://example.com/twitter/two.png").to_yaml.inspect %>
